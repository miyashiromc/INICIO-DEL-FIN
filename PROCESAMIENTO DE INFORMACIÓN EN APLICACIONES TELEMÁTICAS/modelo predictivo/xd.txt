¡Claro que sí! Entendido perfectamente. Crear un conjunto de datos más grande y realista es un excelente paso para hacer tu modelo más robusto y fiable.

He preparado un plan completo para ti. Primero, te proporcionaré un nuevo script de Python (generador_datos.py) dedicado exclusivamente a crear un archivo CSV con 1,500 registros de datos simulados pero realistas. Luego, actualizaré tu script principal (modelo_predictivo.py) para que use este nuevo archivo de datos.

Aquí tienes los archivos:
Generador de Datos de Salarios
14 sept, 9:44 a.m.
Modelo Predictivo Actualizado
14 sept, 9:44 a.m.

Abrir

Paso a Paso para Usar los Archivos
Aquí te explico cómo proceder:

Paso 1: Generar los Datos

Guarda el primer script como generador_datos.py.

Ejecútalo desde tu terminal: python generador_datos.py

Verás un mensaje de éxito, y un nuevo archivo llamado datos_salarios.csv aparecerá en tu carpeta. Este archivo contiene 1,500 filas de datos sobre años de experiencia y salario anual.

Paso 2: Ejecutar el Modelo Predictivo

Guarda el segundo script (el actualizado) como modelo_predictivo.py.

Una vez que el archivo CSV ha sido creado, ejecuta este segundo script: python modelo_predictivo.py

El programa ahora leerá el archivo grande datos_salarios.csv, realizará todo el análisis, entrenará el modelo y te mostrará los resultados.

Mejoras en el Flujo de Trabajo
Usar un conjunto de datos más grande es la mejora principal y más importante. Esto hace que tu modelo sea:

Más Robusto: Con más datos, el modelo aprende un patrón general más fiable y es menos sensible a los datos atípicos.

Más Confiable: Las métricas de evaluación (como R²) son mucho más significativas cuando se calculan sobre un conjunto de prueba más grande.

He mantenido el resto del flujo igual porque los 9 pasos que describiste son el estándar de oro para un proyecto de regresión lineal. ¡Es un procedimiento excelente!

¡Ahora tienes todo lo necesario para trabajar con un conjunto de datos mucho más completo! Si tienes alguna otra duda, házmela saber.# -*- coding: utf-8 -*-
"""
Este script genera un archivo CSV con datos simulados de salarios 
basados en años de experiencia. Es ideal para crear un conjunto de datos
grande y realista para modelos de regresión lineal.
"""

import pandas as pd
import numpy as np

# --- PARÁMETROS DE CONFIGURACIÓN ---
# Puedes cambiar estos valores para generar diferentes datos
NUM_REGISTROS = 1500
NOMBRE_ARCHIVO = 'datos_salarios.csv'
SALARIO_BASE = 35000  # Salario inicial para alguien con 0 años de experiencia
INCREMENTO_POR_AÑO = 2500 # Incremento salarial promedio por cada año de experiencia
RUIDO_SALARIAL = 8000 # Factor de aleatoriedad para simular variaciones reales

def generar_datos():
    """
    Crea un DataFrame de pandas con datos simulados y lo guarda en un archivo CSV.
    """
    print(f"Iniciando la generación de {NUM_REGISTROS} registros...")
    
    # 1. Generar los años de experiencia
    # Generamos valores aleatorios entre 0 y 30 años.
    años_experiencia = np.random.rand(NUM_REGISTROS) * 30
    
    # 2. Calcular el salario teórico (la línea recta perfecta)
    salario_teorico = SALARIO_BASE + (años_experiencia * INCREMENTO_POR_AÑO)
    
    # 3. Añadir "ruido" para hacerlo más realista
    # En el mundo real, el salario no es perfecto y depende de muchos factores.
    # Usamos una distribución normal para simular esta variabilidad.
    ruido = np.random.randn(NUM_REGISTROS) * RUIDO_SALARIAL
    
    # 4. Calcular el salario final, asegurando que no sea negativo
    salario_anual = salario_teorico + ruido
    salario_anual = np.maximum(0, salario_anual) # Nos aseguramos de no tener salarios negativos
    
    # 5. Crear el DataFrame
    df = pd.DataFrame({
        'Años_Experiencia': años_experiencia.round(1),
        'Salario_Anual': salario_anual.round(2)
    })
    
    # 6. Guardar los datos en un archivo CSV
    try:
        df.to_csv(NOMBRE_ARCHIVO, index=False)
        print(f"¡Éxito! Se ha creado el archivo '{NOMBRE_ARCHIVO}' con {len(df)} filas.")
    except Exception as e:
        print(f"Error al guardar el archivo: {e}")

if __name__ == '__main__':
    generar_datos()
